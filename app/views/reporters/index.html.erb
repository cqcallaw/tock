<p id="notice"><%= notice %></p>

<%= render @reporters, style: :summary || "No reporters yet." %>

<div class="row">
  <div class="col-xs-12">
    <h4><%= link_to new_reporter_path do %>
      <i class="glyphicon glyphicon-plus"></i>
      Invite
      <% end %></h4>
  </div>
</div>
