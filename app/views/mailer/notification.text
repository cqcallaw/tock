<%= @greeting %>! We noticed @task.reporter.name hasn't checked in recently. Last activity was on <%= @last_event_time.strftime(@time_format_string) %>.

This link provides more information: <%= link_to @task.reporter.name, root_url + user_reporter_path(:id => @task.reporter.id, :user_id => @task.reporter.user.id) %>

-Tock
