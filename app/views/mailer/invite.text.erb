<%= @greeting %>! I'd like to invite you to scheduled checkins using Tock. Just click on the following link to join for the first time.

<%= url_for(:controller => "checkins", :action => "new", :uuid => @reporter.uuid) %>
